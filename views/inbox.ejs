<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta
    name="viewport"
    content="width=device-width, initial-scale=1.0"
  />
  <title>My Inbox | Footy Feverz</title>
  <link rel="stylesheet" href="/style.css" />
  <style>
    .inbox-container {
      max-width: 800px;
      margin: 2rem auto;
      padding: 1rem;
      text-align: center;
    }
    .card-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 1.5rem;
      justify-content: center;
      margin-top: 2rem;
    }
    .inbox-card {
      width: 320px;
      height: 200px;
      background: #fff;
      border-radius: 1.5rem;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-decoration: none;
      color: #111;
      position: relative;
      transition: transform 0.2s ease;
    }
    .inbox-card:hover {
      transform: scale(1.03);
    }
    .inbox-card h3 {
      font-size: 1.5rem;
      font-weight: 600;
    }
    .notif-badge {
      position: absolute;
      top: 14px;
      right: 20px;
      background: red;
      color: white;
      font-size: 0.9rem;
      padding: 4px 8px;
      border-radius: 999px;
    }

    /* On larger desktops, let cards flex to ~40% of the grid, up to 400px */
    @media (min-width: 1024px) {
      .inbox-card {
        flex: 1 1 40%;
        max-width: 400px;
        width: auto;
        height: 220px;
      }
    }
  </style>
</head>
<body>
  <%- include('partials/_header', {
    headerClass: 'header-home',
    showAuthLinks: true,
    showLeagueLink: false,
    useTeamHeader: false
  }) %>

  <section class="inbox-container">
    <h2>📥 My Inbox</h2>

    <div class="card-grid">
      <!-- Chats card -->
      <a href="/inbox/chats" class="inbox-card">
        <h3>💬 Chats</h3>
        <% if (user.chatNotifications > 0) { %>
          <span class="notif-badge"><%= user.chatNotifications %></span>
        <% } %>
      </a>

      <!-- Mentions card -->
      <a href="/inbox/tags" class="inbox-card">
        <h3>🏷️ Mentions</h3>
        <% if (user.tagNotifications > 0) { %>
          <span class="notif-badge"><%= user.tagNotifications %></span>
        <% } %>
      </a>
    </div>
  </section>
</body>
</html>
